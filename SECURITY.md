# 安全策略

## 支持的版本

只有最新发布版本会收到安全修复。

| 版本 | 支持 |
| --- | --- |
| 最新 `1.x` 发布版 | ✅ |
| 更早版本 | ❌ |

## 报告漏洞

**请不要通过公开 Issue 报告安全问题。**

在维护者已启用私密漏洞报告的 GitHub 仓库中，进入 **Security → Report a vulnerability** 提交。若该入口尚未启用，请选择 Issue 中的“请求私密联系渠道”表单，仅请求维护者提供私密渠道；不要附漏洞细节、个人数据或利用代码。该表单会创建公开 Issue，维护者提供渠道后再私密发送报告。仓库创建后须由维护者在设置中启用私密漏洞报告，本文件不会自动启用它。

报告时请尽量包含：

- 受影响的版本与安装方式（Squirrel 安装版 / 便携版 / 源码运行）
- 复现步骤或概念验证
- 影响范围（例如：读取媒体目录外的文件、绕过协议限制、renderer 逃逸、泄露 GPS / 路径信息）

请**不要**在报告中附带真实私人照片、真实位置信息或他人的个人数据；使用合成样本复现。

## 安全边界

本应用的设计边界见 [使用指南《数据、隐私与原文件安全》](./docs/USAGE.md#数据隐私与原文件安全)。以下情形属于本项目关注的安全问题：

- renderer 在打包版中发起任何网络请求或加载外部资源
- 自定义协议服务了媒体根目录之外或未索引的文件
- 原始 GPS / 经纬度被写入 SQLite、设置、缓存、日志或发送到 renderer
- 重命名、删除操作作用于用户未选择的文件，或绕过回收站永久删除
- 便携模式在数据目录不可写时静默回退到系统目录
- preload 暴露了超出类型化契约的 Node / Electron 能力

以下情形**不**视为漏洞：

- 能访问同一 Windows 账户数据目录的本地用户读取应用数据（应用不做数据加密，见[本地保存什么](./docs/USAGE.md#本地保存什么)）
- 依赖用户主动导入恶意 GeoJSON 且仅影响用户自身会话的问题（但导致崩溃以外的后果仍欢迎报告）
- 未签名安装包触发 SmartScreen 提示

## 披露

修复发布后，我们会在 Release 说明中致谢报告者（除非你希望匿名），并视情况申请 CVE。
